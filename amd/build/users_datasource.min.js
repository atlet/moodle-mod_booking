/**
 * AJAX helper for the inline editing a value.
 *
 * This script is automatically included from template core/inplace_editable
 * It registers a click-listener on [data-inplaceeditablelink] link (the "inplace edit" icon),
 * then replaces the displayed value with an input field. On "Enter" it sends a request
 * to web service core_update_inplace_editable, which invokes the specified callback.
 * Any exception thrown by the web service (or callback) is displayed as an error popup.
 *
 * @module     mod_booking/users_datasource
 * @package
 * @copyright  2023 Andraž Prinčič
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 * @since      4.5
 */
define("mod_booking/users_datasource",["jquery","core/ajax"],(function($,Ajax){return{processResults:function(selector,results){var options=[];return $.each(results,(function(index,data){options.push({value:data.id,label:data.label})})),options},transport:function(selector,query,success,failure){var promise=null,courseid=$(selector).data("courseid");if(!courseid)throw new Error("The attribute data-courseid is required on "+selector);return(promise=Ajax.call([{methodname:"mod_booking_search_users",args:{courseid:courseid,query:query}}]))[0].done(success),promise[0].fail(failure),promise}}}));

//# sourceMappingURL=users_datasource.min.js.map